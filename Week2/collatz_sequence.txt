#include <bits/stdc++.h>

using namespace std;

void coll(long long N) {
  while (true) {
    
    if (N == 1) {
      printf("%d", N);
      break;
    }
    
    printf("%d ", N);
   	N = (N % 2 == 0) ? (N / 2) : ((N * 3) + 1);
    
  }
  
}
int main() {
  long long N;
  
  scanf("%lld", &N);
  
  coll(N);
  
  return 0;
  
}